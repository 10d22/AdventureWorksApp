@model AdventureWorksApp.Models.Photo

@{
    ViewBag.Title = "Create New Photo";
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Create</title>
</head>
<body>
    <div>
        @using (Html.BeginForm("Create", "Photo", FormMethod.Post, new { enctype = "multipart/form-data" }))
        {
            @Html.ValidationSummary(true)
            <span>
                @Html.LabelFor(model => model.Title):
            </span>
            <span>
                @Html.EditorFor(model => model.Title)
                @Html.ValidationMessageFor(model => model.Title)
            </span>
                <span>Upload new @Html.LabelFor(model => model.PhotoFile):</span>
                <input class="pic-upload-input" type="file" name="Image">
            <span class="editor-label">
                @Html.LabelFor(model => model.Description):
            </span>
            <span class="editor-field">
                @Html.EditorFor(model => model.Description)
                @Html.ValidationMessageFor(model => model.Description)
            </span>
                <span class="editor-label">
                    @Html.LabelFor(model => model.Owner):
                </span>
                <span class="editor-field">
                    @Html.DisplayFor(model => model.Owner)
                </span>

                <span class="editor-label">
                    @Html.LabelFor(model => model.CreatedDate):
                </span>
                <span class="editor-field">
                    @Html.DisplayFor(model => model.CreatedDate)
                </span>

            <p>
                <input type="submit" value="Create" />
                @Html.ActionLink("Back to List", "Index")
            </p>
        }
        </div>
        </body>
    </html>
